{
  "name": "@trustip/payments",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@trustip/config": "workspace:*",
    "@trustip/database": "workspace:*",
    "@trustip/stellar": "workspace:*",
    "@trustip/validators": "workspace:*"
  },
  "devDependencies": {
    "@stellar/stellar-sdk": "^16.0.1",
    "typescript": "^5.0.0",
    "vitest": "^2.1.9"
  }
}
