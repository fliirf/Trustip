// @trustip/types — shared cross-package TypeScript types.
// Phase 0 placeholder. Domain types (orders, payments, escrows, payouts, etc.)
// will be added in later phases alongside the database schema.

export {};
